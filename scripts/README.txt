This directory contains small helper scripts for local inspection.

list-build-output.sh
  Print the files generated under a Nix build result directory.

These scripts are optional and are not part of the default GitHub Actions build path.
